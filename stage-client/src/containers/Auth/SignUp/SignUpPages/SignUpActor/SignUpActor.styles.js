import styled from 'styled-components';

const StyledSignUpActor = styled.section`
	.header {
		.desc {
			margin-bottom: 4px;
		}
		.skip-link {
			font-style: italic;
		}
	}
`;

export default StyledSignUpActor;